
import React from 'react';

const DiagnosisModal: React.FC = () => {
  // This component is deprecated and was replaced by the generic InfoModal.
  // This file is kept with a placeholder component to resolve a potential
  // build cache or hidden import issue that was causing a runtime error.
  return null;
};

export default DiagnosisModal;